# 财瞳金融终端 — 产品说明文档

## 文档修订记录

| 版本 | 修订日期 | 修订人 | 修订内容 |
| --- | --- | --- | --- |
| V0.9 | 2026-07-10 | 产品组 | 基于当前仓库代码形成整站现状梳理版 |

## 前置阅读说明

- 目标读者：技术产品、前端研发、服务端研发、测试、运维及技术负责人。
- 阅读顺序建议：产品概述 → 需求总览 → 产品结构 → 全局规则 → 非功能需求 → 验收标准。
- 文档性质：现状基线文档，用于明确当前系统实际行为、数据边界和生产化差距。
- 事实基线：以 2026-07-10 当前仓库代码、环境配置和本地接口联调结果为准。
- 状态标记：`真实数据+回退` 表示优先使用 Tushare、失败后使用本地样例；`前端 Demo` 表示交互仅在浏览器内生效；`展示占位` 表示无完整业务处理。
- 配套文档：`AGENTS.md`、`.env.example`、`skills/abnormal_movement/SKILL.md`、接口实现 `server/tushare-proxy.mjs`。
- 设计稿：暂无独立 Figma 链接，以当前实现为交互基线。
- 注意：本文不代表系统已达到证券交易生产系统标准，也不构成投资建议。

---

## 一、产品概述

### 1.1 产品定位

财瞳金融终端是一套面向金融行情观察与投研演示场景的多模块 Web 原型，提供智询、自选、市场、热度、信号、策略、交易和账户等统一入口，并通过服务端代理接入部分 Tushare 数据。

### 1.2 核心价值

- 提供统一的金融终端信息架构，降低跨页面查看行情、持仓、热度和资讯的切换成本。
- 通过 Tushare 代理保护数据 Token，并为自选、个股详情、热度和异动解读提供动态数据。
- 通过本地回退数据保证演示连续性，避免外部接口异常导致整页不可用。
- 提供异动解读、复盘报告和浏览器打印能力，验证“行情数据到结构化报告”的产品链路。
- 为后续建设真实用户中心、行情中台、策略平台和交易系统提供前端原型与接口基线。

### 1.3 目标用户

| 用户角色 | 当前主要任务 | 使用频次 | 当前适配程度 |
| --- | --- | --- | --- |
| 技术产品人员 | 梳理金融终端流程、验证原型和数据链路 | 每周 | 高 |
| 前端研发 | 开发和联调页面、状态及回退行为 | 每日 | 高 |
| 服务端/数据研发 | 联调 Tushare 代理和字段转换 | 每日 | 中 |
| 测试人员 | 验证真实数据、Mock 回退和交互边界 | 每次发布 | 中 |
| 运维人员 | 配置 Token、端口、CORS 和服务可用性 | 按发布 | 低 |
| 普通投资者 | 浏览行情和生成分析报告 | 演示使用 | 低，不具备生产保障 |

### 1.4 当前阶段

- 当前阶段：`Prototype / Alpha`。
- 产品状态：功能全景已形成，部分行情链路已动态化，核心业务仍大量使用静态数据或前端 Demo。
- 部署状态：前端可发布到 GitHub Pages；Tushare 代理需独立部署。
- 生产判断：不满足真实交易、账户安全、数据合规、用户隔离和高可用要求。

### 1.5 能力状态总览

| 模块 | 核心能力 | 状态 | 主要数据源 |
| --- | --- | --- | --- |
| 智询 | 普通问答、异动解读、复盘、报告打印 | 部分真实 | 固定文案、前端模板、Tushare |
| 自选 | 自选列表、持仓、多股同列、个股详情 | 真实数据+回退 | Tushare + localStorage + Mock |
| 市场 | 多市场导航、股票列表、简版详情 | 展示占位 | Mock |
| 热度 | 涨停、连板、市场情绪、相似股票 | 真实数据+回退 | Tushare + 规则计算 + Mock |
| 信号 | 热点资讯筛选和情感排序 | 展示占位 | 静态资讯 |
| 策略 | 精选/自建策略卡片、会员弹窗 | 展示占位 | 静态策略 |
| 交易 | 模拟策略状态和报告、实盘占位 | 展示占位 | 静态交易数据 |
| 账户 | Demo 账户、资产、账单、收益分析 | 前端 Demo | 静态数据 + localStorage |

---

## 二、需求总览（为什么做）

### 2.1 背景与目标

#### 2.1.1 行业与技术背景

- 金融终端需要同时承载高密度行情、资产视图、资讯、分析和交易入口。
- 金融数据接口通常受 Token、权限、频率和授权范围约束，不能由静态前端直接调用。
- 产品原型需要在真实数据不可用时保持可演示，因此需要明确的回退机制和数据来源标识。
- 当前项目以快速验证产品结构为先，尚未进入生产级用户、交易和合规建设阶段。

#### 2.1.2 当前业务痛点

- 模块多但数据真实性不一致，用户难以判断某个字段来自 Tushare 还是 Mock。
- 前端交互、业务能力和展示占位混合，研发与测试缺少统一的现状定义。
- Tushare 权限失败常被静默转换为空数据，无法区分“无数据”和“无权限”。
- GitHub Pages 与本地 Node 代理是两个部署单元，缺少正式环境拓扑与监控。
- 用户、账户、持仓、模拟交易之间没有统一身份和账本关系。
- 桌面和移动入口均覆盖 8 个模块，但移动端并未完成完整响应式适配。

#### 2.1.3 本文档目标

- 建立当前整站功能、数据、接口和状态的唯一事实基线。
- 为研发改造提供模块边界和数据契约。
- 为测试提供可执行的现状验收标准。
- 为生产化评审暴露安全、合规、性能和依赖风险。
- 为后续版本区分 P0 修复、P1 完善和 P2 规划能力。

### 2.2 范围与边界

#### 2.2.1 本文覆盖

- 桌面端和移动端入口。
- 智询、自选、市场、热度、信号、策略、交易、账户 8 个一级模块。
- Tushare 代理、前端服务层、Mock 回退和本地存储。
- 登录 Demo、报告生成、打印和静态数据处理。
- 当前非功能状态、依赖风险及验收标准。

#### 2.2.2 本文不视为已实现

- 真实大模型问答和 RAG。
- 实时全市场行情中台及 WebSocket 推送。
- 真实券商账户绑定、资金划转、委托、撤单和撮合。
- 策略创建、回测、执行、灰度和风控引擎。
- 正式会员、支付、订单和权益校验。
- 服务端用户中心、数据库、租户隔离和 RBAC。
- 生产级移动端体验、无障碍和国际化。

#### 2.2.3 系统边界

| 层级 | 当前职责 | 边界 |
| --- | --- | --- |
| 浏览器前端 | 页面、交互、模板报告、本地存储、回退 | 不应持有 Tushare Token |
| Tushare 代理 | Token 管理、Tushare 请求、字段转换、30 秒缓存 | 无用户鉴权和持久化 |
| Tushare | A 股行情、基础资料、资金、财务、公告等 | 受积分、接口权限和频率限制 |
| GitHub Pages | 静态前端托管 | 不能运行 Node 代理 |
| 浏览器 localStorage | 保存 Demo 登录、自选、持仓和账户 | 不支持跨端、加密和用户隔离 |

### 2.3 用户画像

#### 2.3.1 技术产品人员

- 特征：理解金融业务与软件交付，关注功能边界和迭代优先级。
- 痛点：无法快速判断页面行为是否真实、接口是否已接入、哪些按钮只是占位。
- 日常任务：演示原型、确认需求、评估生产化差距、组织验收。

#### 2.3.2 前端研发

- 特征：负责 React 页面、状态、图表、Markdown 和数据降级。
- 痛点：数据源分散在组件常量、`src/data`、服务层和 localStorage。
- 日常任务：开发页面、联调代理、处理加载/空/错误状态和响应式布局。

#### 2.3.3 服务端与数据研发

- 特征：负责 Tushare 对接、字段映射、缓存和部署。
- 痛点：当前代理使用单文件原生 HTTP 实现，缺少权限、限流、结构化日志和错误码。
- 日常任务：维护 Token、排查权限、验证交易日、优化请求和缓存。

#### 2.3.4 测试与运维

- 特征：关注可复现性、环境差异、失败降级和服务状态。
- 痛点：多数异常被静默回退，页面没有统一来源标识；旧代理进程可能导致路由版本不一致。
- 日常任务：检查进程、端口、环境变量、接口响应、构建和核心交互。

### 2.4 核心场景

| 场景 | 用户 | 触发 | 核心流程 | 期望结果 |
| --- | --- | --- | --- | --- |
| 异动解读 | 技术产品/测试 | 点击智询“异动解读” | 请求代理 → 获取行情 → 生成 Markdown → 流式展示 → 写入记录 | 明确显示数据来源并生成结构化报告 |
| 自选行情联调 | 前端/数据研发 | 进入自选或刷新 | 读取本地自选 → 批量请求行情 → 合并用户数据 | 行情成功更新，失败时稳定回退 |
| 个股详情验证 | 测试 | 点击自选或持仓股票 | 请求完整详情 → 渲染 K 线、财务和资讯 | 可切换周期，字段无崩溃 |
| 热度验证 | 技术产品 | 进入热度页 | 请求涨跌停数据 → 计算情绪与热度 → 筛选 | 能解释指标口径和回退状态 |
| 账户 Demo | 演示人员 | 添加演示账户 | 输入平台/账号/手机号/验证码 → 写入本地 | 刷新后仍展示，不宣称真实绑定 |
| 代理排障 | 运维 | 页面显示回退数据 | 检查配置 → 健康检查 → 业务路由 → Tushare 权限 | 定位配置、进程、权限或上游错误 |

### 2.5 术语与缩写

| 术语 | 定义 |
| --- | --- |
| Tushare | 金融数据服务，本项目通过服务端代理调用 |
| API 代理 | `server/tushare-proxy.mjs`，用于隐藏 Token 并转换数据 |
| Mock | 仓库内静态样例数据，不代表真实市场状态 |
| Fallback | 接口无配置、失败或无数据时使用本地样例的降级行为 |
| 智询 | 当前 AI 交互入口；普通回答为关键词规则，异动使用动态数据 |
| 异动 | 短期异常涨跌、资金或成交活跃变化 |
| localStorage | 浏览器本地键值存储，当前用于保存 Demo 用户及业务数据 |
| P95 | 95% 请求的响应时间不超过该阈值 |
| SLA | 服务可用性承诺 |
| RBAC | 基于角色的访问控制，当前未实现 |

### 2.6 关键结果（OKR）

> 以下 OKR 用于现状基线验收和下一阶段改进；目标数值为占位值，TBD。

- O1：建立可追溯的整站技术基线。
  - KR1：8 个一级模块的功能、数据源和未实现边界覆盖率达到 100%。
  - KR2：现有 11 个代理路由均具备接口说明和至少 1 条验收标准。
  - KR3：所有动态页面可区分 Tushare 数据与 Mock 回退，目标覆盖率 100%（规划目标）。
- O2：保证 Tushare 链路可联调、可诊断。
  - KR1：本地 `/health` 可用率达到 99%（占位值，TBD）。
  - KR2：核心行情接口成功时返回明确交易日和来源。
  - KR3：上游无权限、超时和无数据三类异常可被区分（规划目标）。
- O3：降低原型被误认为生产系统的风险。
  - KR1：交易、账户、策略、会员和普通 AI 能力均明确标识 Demo/占位状态。
  - KR2：所有投资分析输出包含数据来源和非投资建议边界。

- 假设：技术人员是当前最主要的文档使用者，而不是普通投资者。
- 假设：当前版本优先保留演示连续性，允许接口失败后回退 Mock。
- 假设：Tushare Token 已配置，但不同高级接口权限不完全一致。

---

## 三、业务价值与收益增益

### 3.1 用户任务（Jobs-to-be-done）

| 用户角色 | 待完成任务 | 触发情境 | 期望收益 |
| --- | --- | --- | --- |
| 技术产品 | 判断模块是否可交付 | 需求评审或演示前 | 减少口径争议和错误承诺 |
| 前端研发 | 确定页面状态和数据来源 | 开发、排障、重构 | 降低跨文件检索成本 |
| 服务端研发 | 确定代理路由和上游 API | 接口开发或权限异常 | 快速定位调用链和字段 |
| 测试 | 构造成功、失败和回退用例 | 回归测试 | 提升边界覆盖率 |
| 运维 | 确认前端与代理运行状态 | 发布或故障 | 缩短端口、进程和配置排查时间 |

### 3.2 业务价值与收益增益

#### 3.2.1 对用户的价值

- 效率：通过统一导航集中查看多类金融信息。
- 可理解性：通过来源和状态分类理解数据可信边界。
- 连续性：Tushare 不可用时仍可展示本地样例，保障演示。
- 输出能力：将异动数据转换为可阅读、可打印的 Markdown 报告。

#### 3.2.2 对团队的价值

- 形成研发、测试、运维共用的现状规范。
- 识别技术债，包括单文件代理、静默吞错、无路由和无用户隔离。
- 形成可迁移的数据契约，为后续独立业务后端和数据库建设提供输入。
- 避免将 Mock 指标、规则分数或静态报告误当作真实模型结果。

#### 3.2.3 对生态的价值

- 代理层可继续封装其他合规数据源，减少前端对单一供应商的耦合。
- 统一的前端服务函数允许后续替换 Tushare 或接入内部行情中台。
- 异动 Skill 和报告模板可作为结构化分析能力的输出规范。

### 3.3 当前价值限制

- 无真实 AI、交易、支付和账户同步，不能形成完整商业闭环。
- 无服务端存储，无法实现跨端同步和审计。
- 数据回退缺少统一显著提示，可能造成数据来源误判。
- 多市场入口与实际 A 股代理能力不一致，产品认知存在偏差。

---

## 四、产品结构

### 4.1 功能架构

#### 4.1.1 分层架构

```text
接入层
├─ 桌面入口 index.html
└─ 移动入口 mobile.html

展示与业务层
├─ 智询 ├─ 自选 ├─ 市场 ├─ 热度
└─ 信号 ├─ 策略 ├─ 交易 └─ 账户

前端数据层
├─ services：Tushare 业务接口与回退
├─ data：静态 Mock 数据
└─ localStorage：用户、自选、持仓、账户

代理层
└─ Node HTTP Tushare Proxy
   ├─ Token 隔离
   ├─ 字段转换
   └─ 30 秒内存缓存

外部数据层
└─ Tushare API
```

#### 4.1.2 模块依赖

| 模块 | 依赖 | 输出/影响 |
| --- | --- | --- |
| 智询 | 持仓/传入股票、异动接口、报告模板 | 对话消息、报告记录、打印页 |
| 自选 | localStorage、行情/详情接口 | 自选、持仓、个股详情 |
| 市场 | Mock 股票和指数 | 当前选股、智能分析结果 |
| 热度 | 热度接口、规则计算 | 涨停表、情绪图、相似股 |
| 信号 | 静态热点数据 | 筛选后的资讯表 |
| 策略 | 静态策略数据 | 策略卡和会员弹窗 |
| 交易 | 静态交易数据 | 模拟报告和实盘占位 |
| 账户 | 静态账户、localStorage | Demo 账户和收益展示 |

#### 4.1.3 全局导航

- 桌面端：顶部标题栏 + 左侧 8 项导航 + 底部指数栏。
- 移动端：顶部品牌栏 + 下拉式 8 项导航；市场页显示底部指数栏。
- 当前无 URL Router，模块切换仅修改组件状态。
- 刷新后默认返回“智询”，不支持深链和浏览器前进后退。

### 4.2 核心功能

#### 4.2.1 智询模块

##### 4.2.1.1 页面功能

- 页面入口：一级导航“智询”，桌面和移动默认页。
- 中间区域：消息列表、Markdown 渲染、流式输出、输入框和快捷动作。
- 快捷动作：标的筛选、异动解读、趋势判断、复盘总结、策略选择、风控提示。
- 右侧区域：当前持仓/传入股票列表及报告记录。
- Markdown 支持标题、列表、表格、引用、链接和代码块。
- AI 回复以每 45ms 约 3 个字符的节奏模拟流式输出。

| 状态 | 触发 | 页面表现 |
| --- | --- | --- |
| 初始 | 首次进入 | 展示 Logo、引导问题和欢迎语 |
| 分析中 | 发送问题 | 跳点加载，禁用重复发送 |
| 流式中 | 回复开始 | 逐步渲染 Markdown，自动滚动 |
| 报告生成中 | 点击异动/选择复盘 | 右侧记录显示“生成中” |
| 报告完成 | 模板生成结束 | 中间展示或显示完成提示，记录可点击 |

##### 4.2.1.2 操作流程

1. 普通问答：用户输入 → 关键词匹配 → 固定回答 → 模拟流式输出。
2. 异动解读：点击快捷动作 → 写入用户 Prompt → 创建生成中记录 → 请求异动接口 → 前端生成 Markdown → 中间流式输出 → 更新报告记录。
3. 复盘总结：点击快捷动作 → 选择复盘类型 → 前端拼接报告 → 更新报告记录。
4. 报告打印：点击有内容的记录 → 新窗口渲染 HTML → 用户调用浏览器保存/打印 PDF。

异常分支：

- 未配置 API：异动直接使用当前页面股票。
- API 错误或无股票：使用当前页面股票或输出无异动提示。
- 浏览器阻止新窗口：打印函数返回失败，不创建 PDF。
- 刷新页面：会话消息和新增报告丢失。

权限控制点：当前无登录或会员校验，所有用户可使用。

##### 4.2.1.3 数据逻辑

- 普通回答：`AiChatPage.tsx` 内 `botReplies`。
- 异动行情：`GET /api/abnormal-movement?codes=...`。
- 报告范围：有传入股票时使用传入列表，否则使用当前持仓/自选观察池。
- 异动候选：按涨跌幅绝对值降序，取前 5 只生成报告。
- PE 和 MA 查询结果为确定性前端样例计算，不是 Tushare 财务值。
- 报告记录仅保存在 React 内存，不落 localStorage 或服务器。

示例：

- 输入：`今天券商板块为什么异动？`
- 输出：包含异动基础表征、多维归因、全局归因和短期小结的 Markdown。

##### 4.2.1.4 弹窗/明细模块

- 复盘选择浮层：板块热点、市场复盘、个股复盘、操作复盘。
- 报告记录：生成中不可点击；完成且有内容时可打开打印页。
- Logo 文件选择：读取本地图片，只在当前组件会话内生效。

#### 4.2.2 自选与持仓模块

##### 4.2.2.1 页面功能

- 一级页签：自选、持仓。
- 自选二级页签：股票列表、多股同列。
- 自选表展示代码、名称、价格、涨跌、换手、自选日、自选价格和收益。
- 持仓表展示账户、数量、成本、市值、盈亏、盈亏率和今日涨幅。
- 支持添加自选、手动刷新、账户筛选和进入个股完整详情。
- 当前不支持删除自选或通过 UI 编辑持仓。

##### 4.2.2.2 操作流程

1. 页面加载 → 读取本地自选/持仓 → 请求批量行情 → 合并展示。
2. 添加自选 → 输入代码/名称 → API 搜索或本地搜索 → 点击结果 → 写入 localStorage → 刷新列表。
3. 点击股票 → 进入完整详情页 → 请求聚合详情 → 返回列表。
4. 多股同列 → 选择周期 → 每只股票分别请求图表。

异常分支：

- 行情请求失败：使用静态报价。
- 搜索接口失败：搜索本地样例池。
- 详情接口失败：展示宁德时代样例详情。
- 关联板块/成份股失败：展示空列表。

权限控制点：当前无用户隔离，不同登录用户共享同一浏览器数据。

##### 4.2.2.3 数据逻辑

- 自选收益：`(现价 - 自选价格) / 自选价格 × 100%`。
- 持仓市值：`现价 × 持仓数量`。
- 持仓盈亏：`市值 - 成本价 × 持仓数量`。
- 盈亏率：`盈亏 / 成本金额 × 100%`。
- 自选 Key：`caitong.watchlist.items`。
- 持仓 Key：`caitong.watchlist.holdings.{accountId}`。
- 搜索真实结果的现价当前固定为 0，新增时可能导致自选价格无效。

示例：持仓 100 股、成本 20 元、现价 22 元，市值为 2200 元，盈亏为 200 元，盈亏率为 10%。

##### 4.2.2.4 弹窗/明细模块

- 添加自选弹窗：搜索框、搜索状态、结果列表、关闭操作。
- 个股完整详情：行情、K 线、成交量、MACD、财务、公司、股东、融资融券、分红、大宗交易、资金流和资讯。
- 自选右侧简版详情：详情、图表、资金流、关联板块、成份股。
- 当前简版详情周期按钮只更新选中态，未按周期重取数据；多股同列周期切换会重取。

#### 4.2.3 市场模块

##### 4.2.3.1 页面功能

- 一级市场 Tab：沪深京、板块、指数、港股通、港股、美股、股转、期权期货现货、外汇。
- 二级 Tab：港股通、沪股通、深股通。
- 股票列表字段：代码、名称、现价、涨幅、涨跌、涨速、换手、最高、最低、今开、昨收、量比。
- 右侧简版详情：价格、图表、资金流、关联板块和成份股。
- 顶部可打开“智能分析”面板。

##### 4.2.3.2 操作流程

1. 切换市场 Tab → 仅更新选中样式。
2. 点击股票行 → 更新右侧简版详情。
3. 打开智能分析 → 点击推荐条件或输入文本 → 对本地列表排序/筛选 → 更新市场列表。

异常分支：当前无网络请求和显式异常状态。

权限控制点：无。

##### 4.2.3.3 数据逻辑

- 股票和指数均来自 `src/data/mockData.ts`。
- 市场 Tab 不改变数据集。
- 简版图表和资金流均为固定 Mock。
- 智能分析按关键词执行排序和筛选，不调用 AI。
- 页面文案标注延时行情，但实际数据为静态样例。

示例：输入“异动排行”后，对本地股票按波动规则选出前 5 条，不代表真实全市场排行。

##### 4.2.3.4 弹窗/明细模块

- 智能分析侧栏：推荐条件、消息区、输入框和关闭按钮。
- 无市场搜索、排序配置、分页或真实板块明细弹窗。

#### 4.2.4 热度模块

##### 4.2.4.1 页面功能

- 市场热度、连板趋势、成功率/炸板率、最高板/涨停家数图表。
- 涨停股票表和筛选：全部、2 连板、首板、炸板。
- 相似股票列表和属性面板。
- 支持 Tushare 数据，失败回退 `heatData.ts`。

##### 4.2.4.2 操作流程

1. 进入页面 → 请求 `/api/heat` → 渲染热度和涨停数据。
2. 选择筛选条件 → 前端过滤涨停表。
3. 选择涨停股 → 按题材生成相似股票。
4. 选择相似股 → 展示匹配度、题材和行业地位。

异常分支：接口失败或无数据时整体回退本地热度数据。

权限控制点：部分 Tushare 涨跌停接口可能要求额外积分；当前页面不展示权限错误。

##### 4.2.4.3 数据逻辑

- `2连板`：`连板数 >= 2`。
- `首板`：`连板数 === 1`。
- `炸板`：`开板次数 > 3`，属于当前产品口径。
- 市场热度：`min(100, 涨停家数/80×50 + 最高板/10×30 + 成功率/100×20)`。
- “明涨停概率”是连板、开板和涨幅的启发式评分，不是概率模型。
- 相似度按列表顺序从 96 递减，最低 60，不是算法模型。

##### 4.2.4.4 弹窗/明细模块

- 热度公式通过问号提示展示。
- 当前无涨停股详情跳转和订阅弹窗。
- API 返回的 `subjectBlocks`、`similarStocks` 未被完整使用。

#### 4.2.5 信号模块

##### 4.2.5.1 页面功能

- 日期范围、来源树、情感分排序和查询按钮。
- 资讯表展示标题、摘要、情感分、行业、概念、标的、来源和时间。
- 当前页面实际为热点资讯筛选，不是量化信号或告警中心。

##### 4.2.5.2 操作流程

1. 页面加载 → 对静态资讯去重并排序。
2. 选择日期、来源和排序 → 点击查询 → 前端过滤。
3. 父级来源支持全选、半选和清空子级。

异常分支：日期为文本比较，非法格式没有校验提示。

权限控制点：无。

##### 4.2.5.3 数据逻辑

- 数据源：`src/data/hotspotData.ts`。
- `是否重复=true` 或标题重复的数据被排除。
- HTML 内容通过正则移除标签并截取 60 字。
- 初次加载不应用默认日期，只有点击查询后生效。

##### 4.2.5.4 弹窗/明细模块

- 无资讯详情、收藏、订阅、已读或告警模块。
- 表格中的资讯不代表实时新闻。

#### 4.2.6 策略模块

##### 4.2.6.1 页面功能

- 精选策略 5 条、自建策略 6 条。
- 展示年化收益、累计收益、最大回撤、简介和样例个股。
- 提供查看、编辑、删除、模拟、实盘和新增入口。

##### 4.2.6.2 操作流程

1. 点击策略操作 → 统一打开会员弹窗。
2. 切换会员套餐 → 更新价格和权益展示。
3. 关闭弹窗 → 返回策略页。

异常分支：无接口，无加载或失败状态。

权限控制点：当前会员等级不控制功能；未登录也可访问。

##### 4.2.6.3 数据逻辑

- 策略数据来自 `src/data/tradeData.ts`。
- 无策略参数、规则树、保存、版本、回测或发布数据。
- 展示收益不对应真实回测任务。

##### 4.2.6.4 弹窗/明细模块

- 会员弹窗支持免费、进阶、专业、企业套餐切换。
- “立即开通/联系客服”不创建订单或发起客服会话。
- “新增策略”当前无行为。

#### 4.2.7 交易模块

##### 4.2.7.1 页面功能

- 模式：模拟交易、实盘交易。
- 模拟交易展示策略状态卡和统一报告。
- 报告展示收益、夏普、回撤、资产、曲线和成交记录。
- 实盘交易明确标记待开发。

##### 4.2.7.2 操作流程

1. 点击模拟策略卡 → 进入报告。
2. 点击左上返回箭头 → 返回策略列表。
3. 切换收益/回撤按钮 → 仅切换选中态。
4. 切换实盘 → 展示待开发占位。

异常分支：无业务请求，无异常处理。

权限控制点：无实盘权限、交易密码或风控校验。

##### 4.2.7.3 数据逻辑

- 12 张策略卡和报告均为静态数据。
- 不同策略复用同一报告，只替换策略名称。
- 无委托、成交、资金冻结、持仓更新、T+1、费用和撮合逻辑。
- 收益归因和风险归因明确待开发。

##### 4.2.7.4 弹窗/明细模块

- 模拟报告为页面级明细。
- “结束、移除、设置、刷新”无独立业务行为。
- 点击卡内按钮可能冒泡进入报告。

#### 4.2.8 账户模块

##### 4.2.8.1 页面功能

- 默认 4 个静态证券账户，支持账户切换、金额隐藏和删除。
- 展示总资产、累计收益、现金、股票、理财、昨日收益、收益曲线和账单。
- 提供添加账户、转账和收益详情入口。

##### 4.2.8.2 操作流程

1. 添加账户 → 选择平台 → 输入资金账号、手机号、验证码 → 写入 localStorage。
2. 删除账户 → 更新新增账户列表或默认账户隐藏 ID → 至少保留一个账户。
3. 转账 → 选择转入/转出并输入金额 → 当前确认按钮无处理。
4. 收益详情 → 选择日期范围 → 在静态曲线内过滤。

异常分支：没有短信、券商或转账接口异常；删除账户无二次确认。

权限控制点：账户数据不与登录用户绑定，存在跨用户共享风险。

##### 4.2.8.3 数据逻辑

- 默认账户与资产：`src/data/accountData.ts`。
- 新增账户 Key：`caitong.linkedAccounts`。
- 删除默认账户 ID Key：`caitong.removedAccountIds`。
- 任意非空验证码可完成 Demo 绑定。
- 新账户资产和账单由前端模板生成。
- 账户资产与持仓、模拟交易互不联动。

##### 4.2.8.4 弹窗/明细模块

- 添加账户弹窗：平台、资金账号、手机号、验证码。
- 转账弹窗：转入/转出、金额、可用资金。
- 账单明细：周期按钮不改变数据。
- 收益详情：日期过滤有效，但底层仍是静态曲线。

### 4.3 Tushare 数据集成

#### 4.3.1 调用架构

```text
React 页面
  → src/services/*Service.ts
  → VITE_API_BASE_URL
  → Node Tushare Proxy（默认 8787）
  → https://api.tushare.pro
```

#### 4.3.2 当前能力

- Token 仅由服务端环境变量 `TUSHARE_TOKEN` 读取。
- 前端通过 `VITE_API_BASE_URL` 指向代理。
- 代理对相同 Tushare 请求使用 30 秒进程内缓存。
- CORS 通过 `ALLOWED_ORIGIN` 配置，默认 `*`。
- Tushare 响应被转换为前端中文或业务字段。

#### 4.3.3 上游 API

`trade_cal`、`daily`、`daily_basic`、`stock_basic`、`moneyflow`、`ths_member`、`limit_list_d`、`stk_limit`、`weekly`、`monthly`、`stk_mins`、`anns`、`major_news`、`stock_company`、`income`、`fina_indicator`、`stk_holdernumber`、`margin_detail`、`dividend`、`block_trade`。

#### 4.3.4 代码市场边界

- `6/688/689` 开头追加 `.SH`，其他代码追加 `.SZ`。
- 当前规则不完整支持北交所、港股、美股、期货和外汇。
- 市场页中的港股通样例代码不可直接视为代理已支持。
- 搜索接口基于 A 股 `stock_basic(list_status=L)`。

---

## 五、全局通用规则

### 5.1 全局交互规范

#### 5.1.1 加载态

- 普通异步请求使用 Spinner 或文字提示。
- 智询生成使用跳点动画；流式开始后隐藏跳点。
- 报告记录在生成中显示不可点击状态。
- 规划要求：超过 300ms 的请求显示加载，超过 10s 显示超时提示（占位值，TBD）。

#### 5.1.2 空态

- 无搜索结果：显示无结果提示。
- 无板块/成份股：显示暂无数据。
- 无异动候选：输出固定无异动文案。
- 空态不得用 Mock 冒充真实空结果；需同时显示当前数据源（规划要求）。

#### 5.1.3 错误态

- 当前多数服务捕获异常后静默回退。
- 现状规则：页面不得因 Tushare 异常崩溃。
- 规划规则：错误需区分未配置、网络错误、上游错误、无权限、无数据和回退成功。

#### 5.1.4 表单校验

- 文本问题：去除首尾空格，空文本不发送。
- 股票搜索：支持代码或名称，当前无长度限制。
- Demo 账户：平台、资金账号、手机号和验证码需非空。
- 日期：当前信号页使用字符串比较；规划要求采用日期控件并校验开始日期不晚于结束日期。
- 金额：规划要求大于 0、最多两位小数且不超过可用余额；当前未实现。

#### 5.1.5 提示层级

| 场景 | 建议组件 | 当前状态 |
| --- | --- | --- |
| 操作成功 | Toast | 部分使用页面提示 |
| 可恢复错误 | Toast/Inline Alert | 多数静默回退 |
| 不可逆操作 | Confirm Modal | 删除账户未确认 |
| 复杂详情 | Drawer/Page | 当前多为页面切换 |
| 阻断操作 | Modal | 登录、会员、账户使用弹窗 |

#### 5.1.6 表格规则

- 金额、价格和比率右对齐，名称和代码左对齐。
- 涨跌颜色沿用 A 股习惯：上涨红、下跌绿。
- Markdown 宽表在消息气泡内横向滚动。
- 当前业务表多数无分页、排序和列配置；后续统一默认每页 20 条（占位值，TBD）。

### 5.2 全局权限设计

#### 5.2.1 当前权限状态

- 桌面端提供本地 Demo 登录。
- 用户密码以明文对象存在前端和 localStorage。
- 未登录用户仍可访问全部功能。
- 移动端没有登录入口。
- API 代理无鉴权。
- 会员等级不控制功能和配额。

#### 5.2.2 目标角色矩阵（规划，不代表已实现）

| 功能 | Admin | Operator | Analyst | Viewer |
| --- | --- | --- | --- | --- |
| 系统配置/Token 状态 | 管理 | 查看 | 无 | 无 |
| 行情与热度 | 查看 | 查看 | 查看 | 查看 |
| 智询与报告 | 管理 | 使用 | 使用 | 查看 |
| 数据源诊断 | 管理 | 管理 | 查看 | 无 |
| 用户与权限 | 管理 | 无 | 无 | 无 |
| 审计日志 | 管理 | 查看 | 无 | 无 |

#### 5.2.3 数据权限

- 当前无行级、列级、部门级或租户级权限。
- localStorage Key 未包含 userId，不同本地 Demo 用户共享业务数据。
- 生产化必须在服务端按用户/租户隔离自选、持仓、账户和报告。

#### 5.2.4 审计

- 当前无登录、查询、报告、配置和账户操作审计。
- 规划保留期：操作日志 180 天，安全日志 365 天（占位值，TBD）。

### 5.3 全局配置项

| 配置 | 当前默认值 | 生效方式 | 说明 |
| --- | --- | --- | --- |
| `TUSHARE_TOKEN` | 无 | 代理启动时读取 | 必填，禁止进入前端 |
| `PORT` | `8787` | 代理启动时读取 | 代理监听端口 |
| `ALLOWED_ORIGIN` | `*` | 代理启动时读取 | 生产应配置白名单 |
| `VITE_API_BASE_URL` | 空 | 前端构建时注入 | 为空时直接使用回退数据 |
| Tushare 缓存 TTL | 30 秒 | 代码常量 | 进程内缓存，重启丢失 |
| Vite base | `/caitong-finance/` | 构建配置 | GitHub Pages 子路径 |
| 流式速度 | 45ms/3 字符 | 前端常量 | 模拟流式，不是服务端流 |

### 5.4 数据刷新与缓存规则

- 自选/持仓：进入页面和手动刷新时请求。
- 个股详情：进入详情和切换完整详情周期时请求。
- 多股同列：周期切换时对每只股票请求。
- 热度：组件加载时请求。
- 异动：用户点击快捷动作时请求。
- 市场、信号、策略、交易和账户主数据不请求 Tushare。
- 代理缓存仅按 Tushare 请求参数缓存 30 秒，无跨实例共享。
- 当前无定时轮询和 WebSocket。

### 5.5 数据来源标识规则

- `Tushare真实行情接口`：代理返回有效股票数据。
- `页面当前行情数据`：异动接口未配置或失败后的页面回退。
- 其他服务当前未统一输出来源字段。
- 规划要求：动态模块统一展示来源、交易日、最后更新时间和是否回退。

---

## 六、版本迭代 & 发布规划

### 6.1 版本路线图

| 版本 | 目标 | 核心交付 | 预计时间 |
| --- | --- | --- | --- |
| V0.9 现状基线 | 固化当前事实 | PRD、接口清单、Mock 边界、验收标准 | 2026 Q3 |
| V1.0 技术可用版 | 提升数据可诊断性 | 来源标识、错误码、代理日志、关键缺陷修复 | TBD |
| V1.5 数据平台版 | 建立服务端存储和身份 | 用户中心、报告持久化、自选/持仓同步、RBAC | TBD |
| V2.0 业务闭环版 | 建设真实策略与交易能力 | 需独立立项、券商合规和风控体系 | TBD |

### 6.2 分期交付清单

#### P0：现状基线和高风险修复

- 保持 Tushare Token 仅在服务端。
- 为代理增加结构化错误码和启动版本信息。
- 页面统一显示真实数据或回退状态。
- 修复自选搜索价格为 0、简版周期不请求和旧代理进程不可识别问题。
- 明确标记市场、策略、交易、账户和普通智询的 Demo/Mock 状态。
- 补充核心服务自动化测试。

#### P1：技术能力完善

- 引入 URL Router 和深链。
- 报告、自选、持仓和账户迁移到服务端存储。
- 增加登录鉴权、用户隔离和基础 RBAC。
- 增加代理限流、重试、持久缓存、监控和权限诊断。
- 完成移动端响应式适配。

#### P2：后续独立立项

- 真实大模型和知识库。
- 多市场真实行情。
- 策略创建、回测和执行平台。
- 券商账户、模拟撮合和实盘交易。
- 支付、会员和企业租户。

### 6.3 发布流程

#### 6.3.1 灰度策略

- 当前静态站无用户灰度机制。
- 建议先在测试域名验证代理，再按环境变量切换生产 API。
- 数据源改造应支持按模块开关，先灰度自选与异动，再灰度热度和详情。

#### 6.3.2 回滚预案

- 前端：回滚 GitHub Pages 到上一构建产物。
- 代理：保留上一稳定版本进程或镜像，检查端口后切换。
- 数据：当前无数据库迁移；localStorage 结构变更前需版本化和备份策略。
- 上游异常：关闭 API Base 或启用明确的 Mock 演示模式。

#### 6.3.3 上线 Checklist

- [ ] `npm install` 与 lockfile 一致。
- [ ] `npm run build` 通过。
- [ ] `TUSHARE_TOKEN` 未进入前端构建产物和日志。
- [ ] `/health` 返回 200。
- [ ] 11 个业务接口完成冒烟测试。
- [ ] `VITE_API_BASE_URL` 指向目标代理。
- [ ] `ALLOWED_ORIGIN` 为正式前端域名，不使用 `*`。
- [ ] 首页、智询、自选、热度和个股详情完成回归。
- [ ] 接口失败时页面可降级且有来源提示。
- [ ] GitHub Pages 路径包含 `/caitong-finance/`。

---

## 七、非功能性需求

### 7.1 性能要求

> 当前系统没有性能监控；以下为下一阶段占位目标，TBD。

| 指标 | 目标 | 当前状态 |
| --- | --- | --- |
| 静态首屏 P75 | < 2.5s | 未监控 |
| 代理健康检查 P95 | < 100ms | 未监控 |
| 缓存命中行情 P95 | < 300ms | 未监控 |
| 非缓存行情 P95 | < 3s | 受 Tushare 影响 |
| 异动报告首字时间 | < 5s | 当前请求完成后模拟流式 |
| 单实例并发 | 50 QPS | 占位值，未压测 |

性能边界：

- 当前主 JS Bundle 超过 2MB，Vite 有大 Chunk 警告。
- 个股完整详情并发调用多个 Tushare API。
- 多股同列按股票逐个请求，股票数量增加时请求数线性增长。
- 代理无请求并发限制和批处理调度。

### 7.2 安全要求

#### 7.2.1 当前状态

- Tushare Token 保存在代理环境变量，符合最小安全底线。
- Demo 密码明文保存在前端，不满足生产安全要求。
- API 无身份认证和权限检查。
- CORS 默认允许任意来源。
- localStorage 中账户与用户数据未加密。
- 无 CSRF、速率限制、安全响应头和审计。

#### 7.2.2 生产化要求

- 使用服务端 Session 或短期 Access Token + Refresh Token。
- 密码采用强哈希，不返回前端。
- API 按用户、角色和租户鉴权。
- Token、手机号、资金账号按需脱敏。
- Tushare Token 进入密钥管理系统并支持轮换。
- 代理增加限流、IP/用户配额和异常封禁。
- 金融数据使用需核验数据供应商授权范围。
- 若进入真实证券业务，需单独完成等保、隐私、证券合规和券商接入评审。

### 7.3 可用性要求

- 当前无正式 SLA 和容灾。
- 前端静态站可由 GitHub Pages 提供，但代理为单实例时存在单点故障。
- 现状要求：代理失败不得导致页面崩溃。
- 目标 SLA：非交易原型服务 99.9%（占位值，TBD）。
- 目标容灾：代理至少双实例、健康检查、自动重启和共享缓存（规划）。
- 数据降级必须显式标记，不能把 Mock 展示为实时数据。

### 7.4 可观测性

#### 7.4.1 日志规范

- 当前仅输出代理启动地址，缺少请求日志。
- 规划字段：`requestId`、时间、路由、参数摘要、上游 API、耗时、缓存命中、状态码、错误分类。
- 禁止记录 Tushare Token、密码、完整手机号和资金账号。

#### 7.4.2 监控指标

- 代理进程存活和 `/health`。
- 按路由统计请求量、P50/P95/P99、5xx 比率。
- Tushare 上游错误码、超时、无权限和空数据比例。
- 缓存命中率和缓存项数量。
- 前端回退率、页面错误率和报告生成成功率。

#### 7.4.3 告警阈值

| 告警 | 阈值（占位值，TBD） | 等级 |
| --- | --- | --- |
| 代理不可用 | 连续 3 次健康检查失败 | P1 |
| 业务接口 5xx | 5 分钟 > 5% | P1 |
| Tushare 超时 | 10 分钟 > 10% | P2 |
| Mock 回退率 | 15 分钟 > 20% | P2 |
| P95 延迟 | 10 分钟 > 5s | P2 |

### 7.5 兼容性与可维护性

- 当前技术栈：React 18、TypeScript、Vite、Tailwind、Recharts、ECharts。
- TypeScript 开启严格模式和未使用变量检查。
- 当前无 lint 和自动化测试脚本，唯一标准验证命令为 `npm run build`。
- 建议支持最近两个稳定版 Chrome、Edge、Safari。
- 移动端目标宽度建议从 375px 开始；当前固定宽度组件不满足该目标。

---

## 八、项目依赖与风险说明

### 8.1 上游依赖

| 依赖方 | 内容 | 风险等级 | 说明 |
| --- | --- | --- | --- |
| Tushare | 行情、基础、资金、财务、公告等 | 高 | 权限、积分、频率和字段可能变化 |
| Node 运行环境 | 代理服务 | 中 | 当前使用原生 HTTP 和全局 fetch |
| GitHub Pages | 静态前端 | 中 | 不能托管代理，路径受 base 限制 |
| 浏览器 localStorage | 用户和业务数据 | 高 | 无隔离、无加密、可被清除 |
| 浏览器打印 | PDF 输出 | 低 | 受弹窗拦截和打印实现影响 |

### 8.2 下游依赖

| 依赖方 | 内容 | 风险等级 | 说明 |
| --- | --- | --- | --- |
| 智询异动 | `/api/abnormal-movement` | 高 | 失败会回退页面股票 |
| 自选与持仓 | 批量行情和 localStorage | 高 | 价格与用户成本共同决定盈亏 |
| 个股详情 | 聚合详情接口 | 高 | 部分上游无权限时字段为 0/空 |
| 热度 | 涨跌停高级接口 | 高 | 无权限时可能整体回退 Mock |
| 技术演示 | 统一数据来源认知 | 中 | Mock 未标记会造成误解 |

### 8.3 风险清单

| 风险 | 概率 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| Tushare 高级接口无权限 | 高 | 热度/详情字段为空 | 启动时权限探测、错误分类、来源提示 |
| 旧代理占用端口 | 中 | 新路由返回 404 | 增加版本接口、进程管理和启动前端口检查 |
| 静默回退造成数据误判 | 高 | 将 Mock 当真实行情 | 全局数据来源徽标和回退告警 |
| Token 泄露 | 低/高影响 | 配额滥用和安全事件 | 仅服务端保存、密钥轮换、日志脱敏 |
| 明文 Demo 密码被误用 | 高 | 不符合安全标准 | 明确 Demo，生产前移除本地认证 |
| 多用户共享 localStorage | 高 | 数据串用 | Key 加 userId 仅为过渡，最终迁移后端 |
| 市场入口超出数据能力 | 高 | 港股等错误映射 | 未支持市场标记禁用，建立市场代码规则 |
| 账户/持仓/交易不一致 | 高 | 资产认知错误 | 明确 Demo，生产化建立统一账本 |
| 移动端布局溢出 | 高 | 关键功能不可用 | 独立响应式验收和组件改造 |
| 大 Bundle | 高 | 首屏慢 | 路由懒加载和图表按需加载 |
| 无自动化测试 | 高 | 回归风险 | 增加服务单测、组件测试和 E2E |
| 金融合规不足 | 高 | 无法对外生产使用 | 独立合规评审、免责声明和数据授权 |

### 8.4 假设与待验证项

- 假设：当前项目以技术演示和原型验证为目标，不处理真实资金。
- 假设：Tushare 返回的最新交易日数据满足当前演示需求，不要求盘中实时性。
- 假设：用户接受接口失败时回退，但页面必须清晰标记来源。
- 假设：当前 A 股代码转换足以覆盖已接入自选股票；北交所和跨市场需另行设计。
- 假设：报告记录暂不要求跨会话保存；该假设需产品确认。
- 待验证：所有已使用 Tushare API 的账号权限和调用频率。
- 待验证：GitHub Pages 生产环境的代理公网地址、HTTPS 和 CORS。
- 待验证：普通智询是否计划接入真实大模型，以及数据合规边界。
- 待验证：移动端是否属于正式交付范围，还是仅保留演示入口。
- 待验证：未来用户体系采用单租户、企业多租户还是个人账号模式。

---

## 九、附录

### 9.1 数据字典

#### 9.1.1 基础行情 StockItem

| 字段 | 类型 | 说明 | 备注 |
| --- | --- | --- | --- |
| 序号 | number | 列表序号 | 前端生成 |
| 证券代码 | string | 本地股票代码 | 通常不含交易所后缀 |
| 证券名称 | string | 股票名称 | Tushare 或 Mock |
| 现价 | number | 最新可用价格 | 多数为最近交易日收盘价 |
| 涨幅 | number | 涨跌幅百分比 | 如 1.25 表示 1.25% |
| 涨跌 | number | 涨跌额 | 数值 |
| 涨速 | number | 涨速 | 代理当前固定为 0 |
| 换手 | number | 换手率 | `daily_basic` |
| 最高 | number | 当日最高 | 行情字段 |
| 最低 | number | 当日最低 | 行情字段 |
| 今开 | number | 当日开盘 | 行情字段 |
| 昨收 | number | 前收盘 | 行情字段 |
| 量比 | number | 量比 | `daily_basic` |

#### 9.1.2 自选与持仓

| 实体/字段 | 类型 | 说明 | 持久化 |
| --- | --- | --- | --- |
| 自选.证券代码 | string | 股票代码 | 是 |
| 自选.证券名称 | string | 股票名称 | 是 |
| 自选.自选日 | string | `YYYY-MM-DD` | 是 |
| 自选.自选价格 | number | 加入时价格 | 是 |
| 自选.自选收益 | number | 现价与自选价计算 | 否 |
| 持仓.持仓数量 | number | 用户持仓数量 | 是 |
| 持仓.成本价 | number | 用户成本价 | 是 |
| 持仓.市值 | number | 现价×数量 | 否 |
| 持仓.盈亏 | number | 市值-成本金额 | 否 |
| 持仓.盈亏率 | number | 盈亏/成本金额 | 否 |

#### 9.1.3 异动股票

| 字段 | 类型 | 说明 | 来源 |
| --- | --- | --- | --- |
| 所属板块 | string | Tushare 行业 | `stock_basic` |
| 成交额 | number | 当日成交额 | `daily.amount` |
| 主力净流入 | number | 大单与特大单净额 | `moneyflow` 计算 |
| 直接诱因 | string | 规则生成的短期解释 | 代理规则 |
| 诱因分类 | string | 资金/情绪分类 | 代理规则 |
| 信息来源 | string | 上游字段说明 | 代理 |

#### 9.1.4 热度与情绪

| 字段 | 类型 | 说明 | 备注 |
| --- | --- | --- | --- |
| 连板数 | number | 连续涨停交易日数 | 近 14 个交易日计算 |
| 开板次数 | number | 涨停打开次数 | 上游字段 |
| 明涨停概率 | number | 启发式评分 | 非模型概率 |
| 成功率 | number | 100%-炸板率 | 当前规则 |
| 炸板率 | number | 开板数/涨停数 | 当前规则 |
| 热度 | number | 综合规则分 | 0-100 |

#### 9.1.5 localStorage

| Key | 数据类型 | 用途 | 用户隔离 |
| --- | --- | --- | --- |
| `caitong.auth.users` | `AuthUser[]` | Demo 用户 | 否 |
| `caitong.auth.currentUser` | `AuthUser` | 当前 Demo 登录 | 单浏览器 |
| `caitong.linkedAccounts` | `Account[]` | 新增 Demo 账户 | 否 |
| `caitong.removedAccountIds` | `string[]` | 隐藏默认账户 | 否 |
| `caitong.watchlist.items` | `StoredWatchItem[]` | 自选列表 | 否 |
| `caitong.watchlist.holdings.{accountId}` | `StoredHoldingItem[]` | 账户持仓 | 按账户，不按用户 |

### 9.2 接口清单

| 接口 | Method | 路径 | 参数 | 说明 |
| --- | --- | --- | --- | --- |
| 健康检查 | GET | `/health` | 无 | 返回 `{ok:true}` |
| 批量行情 | GET | `/api/watchlist/quotes` | `codes` | 自选/持仓报价 |
| 热度 | GET | `/api/heat` | 无 | 涨停、情绪和相似股 |
| 异动 | GET | `/api/abnormal-movement` | 可选 `codes` | 异动股票与归因字段 |
| 股票搜索 | GET | `/api/stocks/search` | `q` | 搜索上市 A 股 |
| 简要详情 | GET | `/api/stocks/:code/detail` | `code` | 行情和市场标签 |
| 图表 | GET | `/api/stocks/:code/chart` | `period` | 分时/周期走势 |
| 资金流 | GET | `/api/stocks/:code/moneyflow` | `code` | 主力/散户流入流出 |
| 关联板块 | GET | `/api/stocks/:code/boards` | `code` | 地域、概念、其他 |
| 成份股 | GET | `/api/stocks/:code/members` | `code` | 当前实际为同行业股票 |
| 完整详情 | GET | `/api/stocks/:code/full-detail` | `period` | 行情、F10、财务和资讯聚合 |

#### 9.2.1 标准响应

```json
{
  "data": {}
}
```

错误响应：

```json
{
  "error": "错误信息"
}
```

当前错误仅使用 HTTP 500 和字符串，不具备业务错误码。

### 9.3 埋点需求

> 当前未实现埋点；以下为规划清单。

| 事件名 | 触发时机 | 参数 | 用途 |
| --- | --- | --- | --- |
| `page_view` | 切换一级模块 | page、device | 模块访问 |
| `api_fallback` | 使用 Mock 回退 | route、reason、duration | 数据稳定性 |
| `abnormal_generate` | 点击异动 | codeCount、source | 异动使用率 |
| `report_complete` | 报告生成完成 | type、duration、source | 成功率和耗时 |
| `report_print` | 打开打印页 | reportType | 报告转化 |
| `watchlist_add` | 新增自选 | code、source | 自选使用 |
| `stock_detail_view` | 进入个股详情 | code、period、source | 详情使用 |
| `heat_filter` | 使用热度筛选 | filter、resultCount | 筛选偏好 |
| `account_demo_bind` | 添加 Demo 账户 | platform | Demo 行为分析 |
| `api_error` | 代理请求失败 | route、errorType | 故障分析 |

### 9.4 兼容适配

#### 9.4.1 浏览器

| 浏览器 | 当前预期 | 正式验收建议 |
| --- | --- | --- |
| Chrome | 支持 | 最近 2 个稳定版 |
| Edge | 支持 | 最近 2 个稳定版 |
| Safari | 未系统验证 | 最近 2 个稳定版 |
| Firefox | 未系统验证 | 最近 2 个稳定版 |
| IE | 不支持 | 不纳入 |

#### 9.4.2 移动端

- 独立入口：`mobile.html`。
- 8 个模块通过顶部下拉框切换。
- 当前复用固定宽度桌面组件，存在横向溢出。
- 正式验收需覆盖 375×667、390×844、768×1024。

#### 9.4.3 国际化

- 当前仅中文。
- 金额、日期和交易规则未抽象为地区配置。
- 暂不支持时区、币种和多语言切换。

### 9.5 验收标准

#### 9.5.1 功能验收 Checklist

- [ ] 桌面端默认进入智询，左侧可切换 8 个模块。
- [ ] 移动端默认进入智询，下拉框可切换 8 个模块。
- [ ] 页面刷新后回到智询，URL 不记录当前模块。
- [ ] 未配置 API 时，自选、热度、详情和异动可回退且页面不崩溃。
- [ ] 配置代理后，`/health` 返回 200。
- [ ] 异动接口成功时报告来源为 `Tushare真实行情接口`。
- [ ] 异动报告在中间区域流式渲染完整 Markdown。
- [ ] 异动完成后右侧记录更新，但不自动打开报告。
- [ ] 点击有内容的报告记录可打开打印页。
- [ ] 自选新增后写入 localStorage，刷新后仍存在。
- [ ] 自选和持仓行情失败时使用本地回退。
- [ ] 持仓市值、盈亏和盈亏率计算符合公式。
- [ ] 点击自选/持仓股票可进入完整详情并返回。
- [ ] 多股同列切换周期会重新请求各股票图表。
- [ ] 热度筛选条件符合当前规则。
- [ ] 信号页重复标题不展示，查询后按日期和来源过滤。
- [ ] 策略操作打开会员弹窗，不应产生真实订单。
- [ ] 模拟交易报告使用静态数据，实盘页显示待开发。
- [ ] Demo 账户可本地新增和删除，至少保留一个账户。
- [ ] 转账操作不应被描述为真实资金划转。

#### 9.5.2 API 验收

- [ ] 未知路由返回 404 和 `{error:"Not found"}`。
- [ ] Token 缺失时依赖 Tushare 的接口返回明确错误。
- [ ] 相同上游请求在 30 秒内可命中内存缓存。
- [ ] 代理不得在响应或日志中输出 Token。
- [ ] 股票代码转换对沪深 A 股符合当前规则。
- [ ] 港股和北交所不得被标记为已完整支持。
- [ ] 完整详情部分接口无权限时页面仍可渲染。

#### 9.5.3 性能验收

- [ ] 执行生产构建并记录 Bundle 大小。
- [ ] 对 `/health`、批量行情、异动和完整详情执行基础压测。
- [ ] 验证 10 只股票多股同列时页面不崩溃。
- [ ] 验证长 Markdown 流式输出时滚动和表格渲染不卡死。
- [ ] 具体门槛采用 7.1 占位目标，发布前由技术负责人确认。

#### 9.5.4 安全验收

- [ ] 前端构建产物不包含 `TUSHARE_TOKEN`。
- [ ] 正式环境 CORS 不使用 `*`。
- [ ] 日志不记录密码、Token、完整手机号和资金账号。
- [ ] 若仍保留 Demo 登录，页面明确标识非生产认证。
- [ ] 任何真实资金或交易能力上线前完成独立安全与合规评审。

#### 9.5.5 UAT 签字流程

1. 产品确认功能状态分类和文案。
2. 前端确认页面行为与回退一致。
3. 服务端确认接口和 Tushare 权限。
4. 测试提交功能、接口和兼容性报告。
5. 运维确认环境变量、域名、CORS 和监控。
6. 技术负责人签字后发布。

### 9.6 参考资料

- 当前仓库 `AGENTS.md`。
- `skills/abnormal_movement/SKILL.md`。
- `skills/abnormal_movement/assets/abnormal-report-template.md`。
- Tushare 官方接口文档：https://tushare.pro/document/2
- Product Compass PRD Template：https://www.productcompass.pm/p/prd-template
- AI PRD Template：https://www.productcompass.pm/p/ai-prd-template
- 当前实现关键入口：`src/App.tsx`、`src/mobile/App.tsx`、`server/tushare-proxy.mjs`。

---

## 文档结论

财瞳金融终端当前可作为金融产品结构、部分行情数据和报告生成链路的技术原型。自选、持仓行情、个股详情、热度和异动解读已形成 Tushare 动态数据与本地回退并存的能力；市场、信号、策略、交易、账户和普通智询仍主要是 Mock、前端 Demo 或展示占位。下一阶段不应直接扩张更多展示功能，而应优先解决数据来源标识、错误可诊断性、身份与数据隔离、自动化测试和代理生产化问题。
